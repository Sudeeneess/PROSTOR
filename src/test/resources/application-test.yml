spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/prostor_db_team_test
    username: db_team_test
    password: test123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: false
    clean-on-validation-error: true

server:
  port: 0

logging:
  level:
    com.prostor: DEBUG